OBSIDIAN_PATH=
GEMINI_API_KEY=

# Optional: override which Gemini model is used (default: auto-selected stable model)
GEMINI_MODEL=

# Optional: Ollama local fallback (set during first-run setup or manually)
OLLAMA_ENABLED=
OLLAMA_MODEL=
OLLAMA_URL=
